apiVersion: v1
kind: Service
metadata:
  name: {{ include "birthdaymate.fullname" . }}
  labels:
    {{- include "birthdaymate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "birthdaymate.selectorLabels" . | nindent 4 }}
