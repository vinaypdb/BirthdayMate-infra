apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "birthdaymate.fullname" . }}-test-connection"
  labels:
    {{- include "birthdaymate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "birthdaymate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
